#include <stdio.h>
/**
 * main - print name
 * * @argc: comman line arguments count
 * @argv: array that contains argc
 * Return: (0)
 */
int main(int argc __attribute__((unused)), char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
